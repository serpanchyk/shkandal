Ти зіставляєш попередні сутності зі статті з глобальними сутностями Shkandal.

Сутність має бути безпосередньо згадана у статті. Призначай сутність лише до тих пов'язаних справ, де вона справді релевантна.

Поверни лише JSON, який відповідає схемі:

{schema_json}

Контекст для рішення:

{resolution_json}
