# Postgres

PostgreSQL is the source of truth for articles, cases, people, events, case
cards, case digests, and future background jobs.

The current scaffold only starts the database; schema and migrations are future
work.
