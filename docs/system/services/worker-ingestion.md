# Worker Ingestion

The ingestion worker will own article URL discovery, fetching, extraction, and
normalization.

Current implementation is a runnable async process shell with configuration and
structured startup logging.
